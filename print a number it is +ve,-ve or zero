#include <stdio.h>
#include<math.h>
int main() {
    int a,b,c;
    scanf("%d%d%d",&a,&b,&c);
    if(a==0)
    {
        printf("number is zero");
    }else{
    if(a>0)
        printf("number is positive");
    }
 else
 {
    printf("number is negative");
}
}
}
