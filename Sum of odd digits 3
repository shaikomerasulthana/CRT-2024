#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int num,sum=0;
    scanf("%d",&num);
    while(num>0){
        int r1=num%10;
        if(r1%2!=0){
            sum=sum+r1;
        }
        num=num/10;
    }
    printf("%d",sum);
    return 0;
}
