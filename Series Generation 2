#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    int a=7;
    scanf("%d",&n);
    for(int i=1;i<=n;i++){
        printf("%d ",a);
        if(i%2!=0)
        {
            a=a-2;
        }
    else{
        a=a+3;
    }
}
    return 0;
}
