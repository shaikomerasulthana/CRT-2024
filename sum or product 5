#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int a,b;
    scanf("%d%d",&a,&b);
    int r=a*b;
    if(r<=1000)
        printf("%d",r);
    else
        printf("%d",a+b);
    return 0;
}
